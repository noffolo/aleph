name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Allow manual triggering

env:
  GO_VERSION: '1.26'
  NODE_VERSION: '20'

jobs:
  go-backend:
    name: Go Backend
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum

      - name: Set GOTOOLCHAIN
        run: echo "GOTOOLCHAIN=local" >> $GITHUB_ENV

      - name: Verify Go module
        run: go mod download && go mod verify

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --timeout=5m
          working-directory: ./

      - name: Run Go tests with race detector and coverage
        run: |
          set -o pipefail
          go test -race -count=1 -coverprofile=coverage.out ./... 2>&1 | tee test-output.txt
          go tool cover -func=coverage.out | tail -1

      - name: Check Go coverage threshold (excludes proto)
        run: |
          go tool cover -func=coverage.out | grep -v ".pb.go" | grep -v "connect.go" | awk '{print $NF}' | grep -E "^[0-9]" | sed 's/%//' | awk '{s+=$1; c++} END {avg=s/c; print "Go coverage:", avg "%"; if (avg < 60) { print "BELOW THRESHOLD (60%)"; exit 1 } else { print "ABOVE THRESHOLD" }}'

      - name: Run go vet
        run: go vet ./...

      - name: Run govulncheck (Go vulnerability scan)
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

      - name: Upload test results (optional)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: go-test-results
          path: test-output.txt

      - name: Build Go binary
        run: go build -o aleph ./main.go

      - name: Verify binary exists
        run: test -f ./aleph && echo "Binary exists"

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: npm audit (high severity gate)
        working-directory: frontend
        run: npm audit --audit-level=high

      - name: Check ESLint config exists
        working-directory: frontend
        run: test -f eslint.config.js && echo "ESLint config present"

      - name: TypeScript type check
        working-directory: frontend
        run: npx tsc --noEmit

      - name: Run vitest with coverage
        run: npx vitest run --pool=forks --coverage
        working-directory: frontend
        env:
          CI: true

      - name: Check frontend coverage threshold
        working-directory: frontend
        run: |
          COVERAGE=$(node -e "const r=require('./coverage/coverage-summary.json'); console.log(r.total.statements.pct)")
          echo "Frontend coverage: ${COVERAGE}%"
          if (( $(echo "$COVERAGE < 55" | bc -l) )); then
            echo "BELOW THRESHOLD (55%)"
            exit 1
          fi
          echo "ABOVE THRESHOLD"

      - name: Build frontend (quiet)
        run: npm run build -- --logLevel error
        working-directory: frontend

      - name: Run Playwright tests
        run: npx playwright test
        working-directory: frontend
        env:
          CI: true
          E2E_TEST_API_KEY: "test-e2e-key-do-not-use-in-prod"

  integration-tests:
    name: Integration Tests (Postgres)
    runs-on: ubuntu-latest
    needs: [go-backend]

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: aleph
          POSTGRES_PASSWORD: aleph-test
          POSTGRES_DB: aleph
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum

      - name: Set GOTOOLCHAIN
        run: echo "GOTOOLCHAIN=local" >> $GITHUB_ENV

      - name: Run integration tests
        run: go test -race -count=1 -tags=integration -v ./internal/app/...
        env:
          ALE_POSTGRES_DSN: "postgres://aleph:aleph-test@localhost:5432/aleph?sslmode=disable"
          ALE_KEY_ENCRYPTION_KEY: "test-encryption-key-32bytes!!"
          ALE_DUCKDB_PATH: ":memory:"

  nlp-test:
    name: NLP Python Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: nlp/requirements.txt

      - name: Install Python dependencies
        working-directory: nlp
        run: pip install -r requirements.txt

      - name: Run Python NLP unit tests
        working-directory: nlp
        run: python -m pytest -v --tb=short 2>&1 | tee pytest-output.txt

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nlp-test-results
          path: nlp/pytest-output.txt

  contract-tests:
    name: Contract Tests (Go ↔ Python NLP)
    runs-on: ubuntu-latest
    needs: [go-backend, frontend]

    env:
      NLP_SIDECAR_ADDR: localhost:8001

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum

      - name: Set GOTOOLCHAIN
        run: echo "GOTOOLCHAIN=local" >> $GITHUB_ENV

      - name: Start NLP sidecar
        run: docker compose up -d aleph-python-sidecar

      - name: Wait for sidecar to be healthy
        run: |
          for i in $(seq 1 30); do
            if nc -z localhost 8001 2>/dev/null; then
              echo "Sidecar ready after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "Sidecar did not start in time"
          docker compose logs aleph-python-sidecar
          exit 1

      - name: Run Go contract tests
        run: go test -tags=contract -v -count=1 ./internal/nlp/...

      - name: Stop sidecar
        if: always()
        run: docker compose down aleph-python-sidecar

  benchmarks:
    name: Benchmarks (Performance Regression)
    runs-on: ubuntu-latest
    needs: [go-backend]
    continue-on-error: true  # warning-only gate, does not block PR

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum

      - name: Set GOTOOLCHAIN
        run: echo "GOTOOLCHAIN=local" >> $GITHUB_ENV

      - name: Run Go benchmarks
        run: |
          go test -bench=. -benchmem -count=5 -benchtime=500ms ./... > ./bench-current.txt 2>&1
          cat ./bench-current.txt

      - name: Compare with baseline
        run: |
          if [ -f .github/benchmarks/baseline.txt ]; then
            bash .github/scripts/bench-compare.sh ./bench-current.txt .github/benchmarks/baseline.txt
          else
            echo "No baseline found — storing current as baseline"
            cp ./bench-current.txt .github/benchmarks/baseline.txt
          fi

      - name: Update baseline (main only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          cp ./bench-current.txt .github/benchmarks/baseline.txt
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .github/benchmarks/baseline.txt
          git commit -m "chore(ci): update benchmark baseline" || echo "No changes to commit"
          git push || echo "No push needed"

      - name: Upload benchmark results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: bench-current.txt

  docker:
    name: Docker Build Verification
    runs-on: ubuntu-latest
    needs: [go-backend, frontend, contract-tests]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: docker build . --tag aleph-v2:ci --progress=plain

      - name: Verify Docker Compose
        run: docker compose config

      - name: Trivy container vulnerability scan
        run: |
          docker run --rm aquasec/trivy image --severity=CRITICAL --exit-code=1 aleph-v2:ci

      - name: Save Docker image metadata
        run: |
          echo "Image built successfully at $(date)" > docker-metadata.txt
          docker images aleph-v2:ci >> docker-metadata.txt

      - name: Upload Docker metadata
        uses: actions/upload-artifact@v4
        with:
          name: docker-metadata
          path: docker-metadata.txt

  docker-push:
    name: Docker Push
    runs-on: ubuntu-latest
    needs: docker
    # Only push to registry on pushes to main (not on PRs or workflow_dispatch)
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/aleph-v2
          tags: |
            type=raw,value=latest
            type=sha,prefix={{branch}}-,format=short

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  ci-summary:
    name: CI Summary
    runs-on: ubuntu-latest
    needs: [go-backend, frontend, integration-tests, nlp-test, benchmarks, contract-tests, docker, docker-push]
    
    steps:
      - name: Check CI status
        run: |
          echo "CI Pipeline completed"
          echo "Go Backend: ${{ needs.go-backend.result }}"
          echo "Frontend: ${{ needs.frontend.result }}"
          echo "Benchmarks: ${{ needs.benchmarks.result }}"
          echo "Contract Tests: ${{ needs.contract-tests.result }}"
          echo "Docker: ${{ needs.docker.result }}"
          
          if [[ "${{ needs.go-backend.result }}" == "failure" ]]; then
            echo "⚠️ Go Backend tests may be failing - check test outputs"
          fi
